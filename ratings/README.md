Drop exported rating files here (any `*.json` array of rating records).

1. In the app, open `#ratings` and hit **Download** (or **Copy JSON** and paste into a file).
2. Save it in this directory, then run `bun run ratings:report`.
